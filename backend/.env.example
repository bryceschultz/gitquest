# Copy to .env (never commit .env). MongoDB Atlas free tier works.
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/gitquest
JWT_SECRET=<generate-a-long-random-string>
CLIENT_URL=https://<your-frontend-host>
PORT=5001
